spring.application.name=Stock-Pulse

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/stock_pulse
spring.datasource.username=root
spring.datasource.password=yash@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

#Redis
spring.redis.host=localhost
spring.redis.port=6379
spring.cache.redis.time-to-live=60000  
# Cache expiry in milliseconds (60s)

#Restart Redis and Your Application
#If the issue persists, restart Redis:
#
#powershell
#taskkill /F /IM redis-server.exe
#redis-server.exe